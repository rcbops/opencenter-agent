# roush-agent - a modular, pluggable host agent in python
#

description     "roush-agent daemon"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respwan limit 10 5
umask 022

script
    DAEMON_ARGS="-c /etc/roush.agent.conf"
    [ -e /etc/default/roush-agent ] && . /etc/default/roush-agent

    /usr/bin/roush-agent ${DAEMON_ARGS}
end script
